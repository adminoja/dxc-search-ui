import { RunScriptsDirectiveDirective } from "./run-scripts-directive.directive";


describe('RunScriptsDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RunScriptsDirectiveDirective(null);
    expect(directive).toBeTruthy();
  });
});
