import { ConverseTimePipe } from './converse-time.pipe';

describe('ConverseTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ConverseTimePipe();
    expect(pipe).toBeTruthy();
  });
});
