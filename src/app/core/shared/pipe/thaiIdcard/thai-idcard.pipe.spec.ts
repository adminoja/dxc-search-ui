import { ThaiIdcardPipe } from './thai-idcard.pipe';

describe('ThaiIdcardPipe', () => {
  it('create an instance', () => {
    const pipe = new ThaiIdcardPipe();
    expect(pipe).toBeTruthy();
  });
});
