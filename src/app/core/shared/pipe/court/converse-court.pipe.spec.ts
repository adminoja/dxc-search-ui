import { ConverseCourtPipe } from './converse-court.pipe';

describe('ConverseCourtPipe', () => {
  it('create an instance', () => {
    const pipe = new ConverseCourtPipe();
    expect(pipe).toBeTruthy();
  });
});
