import { ObjectvaluePipe } from './objectvalue.pipe';

describe('ObjectvaluePipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectvaluePipe();
    expect(pipe).toBeTruthy();
  });
});
