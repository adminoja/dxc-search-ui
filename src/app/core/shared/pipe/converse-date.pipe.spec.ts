import { ConverseDatePipe } from './converse-date.pipe';

describe('ConverseDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ConverseDatePipe();
    expect(pipe).toBeTruthy();
  });
});
