import { ConverseStatusPipe } from './converse-status.pipe';

describe('ConverseStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ConverseStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
