import { ConverseNullPipe } from './converse-null.pipe';

describe('ConverseWeightPipe', () => {
  it('create an instance', () => {
    const pipe = new ConverseNullPipe();
    expect(pipe).toBeTruthy();
  });
});
