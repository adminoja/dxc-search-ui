import { ThainumberPipe } from './thainumber.pipe';

describe('ThainumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ThainumberPipe();
    expect(pipe).toBeTruthy();
  });
});
