import { CountlengthPipe } from './countlength.pipe';

describe('CountlengthPipe', () => {
  it('create an instance', () => {
    const pipe = new CountlengthPipe();
    expect(pipe).toBeTruthy();
  });
});
